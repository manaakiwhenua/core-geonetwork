step 1. clone repo

```sh
git clone git@github.com:manaakiwhenua/core-geonetwork.git (or https://github.com/manaakiwhenua/core-geonetwork.git)
```

step 2. cd into folder
```sh
cd core-geonetwork
```

step 3. docker compose up
``sh
docker compose up
``